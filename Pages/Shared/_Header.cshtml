@using Microsoft.AspNetCore.Identity
@inject SignInManager<Models.Users.User> SignInManager
@inject UserManager<Models.Users.User> UserManager
@{
    var isSignedIn = SignInManager.IsSignedIn(User);
    var isAdmin = isSignedIn && User.IsInRole("Admin");
}

@functions{
    public string active(string navPage)
    {
        var currentPage = ViewContext.RouteData.Values["page"].ToString()!;
        return currentPage == navPage ? "active" : "";
    }
}

<h3 class="masthead-brand">
    <a class="navbar-brand" asp-area="" asp-page="/Index">Web Gallery</a>
</h3>
<nav class="nav nav-masthead justify-content-center">

    @* TODO: set active nav-link *@
    @* Etc. *@
    <a class="nav-link @active("/About")" asp-area="" asp-page="/About">About us</a>
    <span class="nav-link nav-separator"></span>

    @* Media-access *@
    <a class="nav-link @active("/Media/Albums")" asp-area="" asp-page="/Media/Albums">Albums</a>
    <a class="nav-link @active("/Media/Videos")" asp-area="" asp-page="/Media/Videos">Videos</a>
    @if (isSignedIn)
    {
        <a class="nav-link @active("/Media/Upload")" asp-area="" asp-page="/Media/Upload">Upload</a>
        @if (isAdmin)
        {
            <a class="nav-link @active("/Media/Moderation")" asp-area="" asp-page="/Media/Moderate">Moderation</a>
        }
    }
    <span class="nav-link nav-separator"></span>

    @* User-management *@
    @if (isSignedIn)
    {
        <a class="nav-link @active("/Users/Profile/Edit")" asp-area="" asp-page="/Users/Profile/Edit">Logged in as <u>@User.Identity.Name</u></a>
        <a class="nav-link  " asp-area="" asp-page="/Users/Logout">Log out</a>
        <form method="POST" asp-area="" asp-page="/Users/Logout" id="LogoutForm">
            <input type="hidden">
        </form>
        @if (isAdmin)
        {
            <a class="nav-link @active("/Users/Invite")" asp-area="" asp-page="/Users/Invite">Editor Invitation</a>
            <a class="nav-link @active("/Users/Ban")" asp-area="" asp-page="/Users/Ban">Editor Ban</a>
        }
    } else
    {
        <a class="nav-link @active("/Users/Login")" asp-area="" asp-page="/Users/Login">Login</a>
    }

</nav>
<!--
<nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
    <div class="container">
        <a class="navbar-brand" asp-area="" asp-page="/Index">web_gallery</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        @* Menu elements *@
        <div class="navbar-collapse collapse d-sm-inline-flex flex-sm-row-reverse">
            @* User Menu *@
            <ul class="navbar-nav flex-grow-1">
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-page="/About">About</a>
                </li>
                
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-page="/Media/Albums">Albums</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-page="/Media/Videos">Videos</a>
                </li>
            </ul>
            @* Editor menu *@
            <ul class="navbar-nav flex-grow-1">
                @if (SignInManager.IsSignedIn(User))
                {
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-area="" asp-page="/Users/Profile/Edit">@User.Identity.Name</a>
                    </li>
                    <li class="nav-item">
                        <form method="POST" asp-area="" asp-page="/Users/Logout">
                            <input type="submit" value="Logout">
                        </form>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-area="" asp-page="/Media/Upload">Upload</a>
                    </li>
                    @* Admin menu *@
                    @if (User.IsInRole("Admin"))
                    {
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-page="/Users/Invite">Invite new editor</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-page="/Users/Ban">Ban editor</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-page="/Media/Moderate">Moderate media</a>
                        </li>
                    }
                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-area="" asp-page="/Users/Login">Login</a>
                    </li>
                } 
            </ul>

        </div>
    </div>
</nav>
-->