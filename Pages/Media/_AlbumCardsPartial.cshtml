@model List<Models.Media.Album>
@{
}

@foreach (var album in @Model)
{
    <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
    <div class="card mb-4 box-shadow bg-secondary">
        <img class="card-img-top" data-src="TODO" alt="Thumbnail">
        <div class="card-body">
        <p class="card-text">
            @album.Details.Description
            @album.Details.Name
        </p>
        <div class="d-flex justify-content-between align-items-center">
            <div class="btn-group" style="">
            <a href="/Media/Albums/View?id=@album.Id" role="button" class="btn btn-sm btn-outline-light">View</a>
            <a href="#" role="button" class="btn btn-sm btn-outline-light disabled">Edit</a>
            </div>
            <small class="text-muted">
            @album.Timestamp.ToString("yyyy.MM.dd.")
            </small>
        </div>
        </div>
    </div>
    </div>   
}